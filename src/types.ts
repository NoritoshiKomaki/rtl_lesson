export type Framework = { id: number; item: string };

export type User = {
    address: any;
    company: any;
    email: string;
    id: number;
    name: string;
    phone: string;
    username: string;
    website: string;
};

export type CustomCounterState = {
    mode: number;
    value: number;
    username: string;
};
